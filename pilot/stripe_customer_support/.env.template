# Copy to .env in this folder and fill in. NEVER commit the real .env.
# All keys are TEST MODE. No live keys, no sk_live, no production access.

# Stripe restricted key in TEST MODE (starts with rk_test_).
# Scope it to only the write actions you are testing (see README step 2).
STRIPE_SECRET_KEY=rk_test_...

# Anthropic key for Claude Sonnet 5 (provider: sonnet5)
ANTHROPIC_API_KEY=sk-ant-...

# Google AI Studio key for Gemini 3.5 Flash (provider: gemini-flash)
GEMINI_API_KEY=...

# Placeholder only. support_agent.py checks this at import time, but we override
# the model to LiteLLM and disable tracing, so OpenAI is never actually called.
OPENAI_API_KEY=placeholder-not-used-litellm
